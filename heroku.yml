# Heroku deployment config
# Automatically provisions PostgreSQL add-on

build:
  languages:
    - python

run:
  web: uvicorn api.main:app --host 0.0.0.0 --port $PORT
  bot: python main.py

addons:
  - plan: heroku-postgresql
    as: DATABASE
