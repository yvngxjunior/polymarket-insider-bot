"""FastAPI REST API for PolyInsider Bot.

Provides read-only endpoints to query:
- Open positions (TP/SL status)
- Portfolio state (capital, P&L, drawdown)
- Trade history (executed/skipped/failed)
- Tracked wallets (scores, win rates)

Docs: http://localhost:8000/docs (Swagger UI)
"""
